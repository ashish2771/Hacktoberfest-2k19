// this program counts the number of elements in the word
#include<stdio.h>
#include<string.h>
int main()
{
    int i;
    char a[500];
    printf("enter the string\n");
    scanf("%s",a);
    for (i=0;a[i] != '\0';i++)
    {}
    printf("the number of elements in the word is %d\n",i);
}